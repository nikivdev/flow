# Changelog

## [Unreleased]

### Added
- Add git-like `clone` command with docs
- Add interactive `~/run` repo setup to installer
- Add `run-exec` task for one-shot run repo onboarding
- Add run repo management tasks with nested project support
- Add one-command `update-deps` pipeline for vendored crates
- Unify interactive command execution with proper PTY, raw mode, and SIGWINCH handling

### Changed
- Force SSH for GitHub clone URLs in repos commands
- Make vendored warning hygiene deterministic and auditable
- Capture crate provenance in inhouse vendor metadata

### Fixed
- Fix ad-hoc signing of installed binaries on macOS
- Fix inhouse tag creation to be non-interactive
- Fix `update-deps` to handle empty args and `--` passthrough
- Fix run monorepo root sync before executing subdir tasks
- Fix installer to hard-verify fallback binary before reporting success
- Fix installer to verify installed binary and retry alternate Mac architecture

## [0.1.2] - 2026-02-15

### Added
- Add registry package installation to `~/.flow/bin` by default
- Add `FLOW_REGISTRY_URL` to shell config during install
- Add quality gates, skill management, and usage analytics
- Add skill fetching with sequence-backed retries
- Add review report generation and re-run command for commit reviews
- Add PR edit watcher and gitignore policy commands
- Add commit-queue PR helpers for jj bookmark heads
- Add `--all` approve flag for batch operations

### Changed
- Default `f install` to always try registry first via myflow.sh
- Default registry URL to https://myflow.sh
- Gate commit-queue approvals on review issues and record issues as project todos
- Change default version to canary in install.sh
- Silence base ingest stderr when base doesn't support ingest

## [0.1.1] - 2026-02-07

### Added
- Add canary release support with `--canary`/`--stable` upgrade flags
- Add macOS code signing and release signing management
- Add staged diff secret scanning with AI auto-fix and queue policy
- Add `f ask` command for AI task suggestions
- Add diff bundle management with environment variable handling and stash support
- Add `f prod` command for production deployments
- Add `f ext` command for external dependency management
- Add `f migrate` command to move projects with symlinks and AI sessions
- Add OAuth device auth, health checks, and install improvements
- Add archive command to save project snapshots
- Add supervisor daemon manager with restart policies, boot daemons, and readiness checks
- Add web deployment and local env backend
- Add `f install` command and registry release support
- Add `f code` command for browsing local repos and migrating AI sessions
- Add `f new` with fuzzy template selection and auto-cd
- Add `f shell-init` for shell integration
- Add commit bookmarking with mark/unmark actions
- Add docs hub and project template creation
- Add AI context loading and session history tracing
- Add trace viewing with RocksDB storage for task execution monitoring
- Add `info` command, copy support for `migrate`, and reworked `tasks` command
- Add dev installer script for local macOS builds with Jazz/Groove crates
- Add prefix matching for task names

### Changed
- Replace GoReleaser with Rust-based cross-platform build workflow
- Separate AI database from flow database
- Remove desktop app and tscp tool
- Remove npm support, use registry instead
- Rename 1focus to cloud across codebase

### Fixed
- Fix macOS code signing in CI workflows
- Fix session resume to skip empty sessions
- Fix database lock error handling in task run recording
- Fix `apply_diff` to call `apply_diff_content` correctly
- Use rustls for reqwest to fix Linux ARM CI builds
- Warn when symlink target exists as a directory
- Auto-install project dependencies via Homebrew

## [0.1.0] - 2026-01-07

Initial public release of Flow CLI.

_Generated by [Changesmith](https://changesmith.dev)_
